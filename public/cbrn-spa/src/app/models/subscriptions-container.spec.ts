import { SubscriptionsContainer } from './subscriptions-container';

describe('SubscriptionsContainer', () => {
  it('should create an instance', () => {
    expect(new SubscriptionsContainer()).toBeTruthy();
  });
});
